COMMENT
//ANALYTIC Results_test_vss_fee
  Upload vss student fee income test results.
//RESULT LOG
END

COMMENT
INDEX
ODBC64
    Sqlite_vss_test_fee->W:\Vss_fee\Vss_test_fee.sqlite
ENVIRONMENT
VSS
    STUDENT FEE INCOME TESTS - REGISTRATION FEES
        REGISTRATION FEE ABNORMAL (CONTACT)
        REGISTRATION FEE NEGATIVE (CONTACT)
        REGISTRATION FEE NULL (CONTACT)
        REGISTRATION FEE ZERO (CONTACT)
    STUDENT FEE INCOME TESTS - QUALIFICATION FEES
        QUALIFICATION FEE NULL (CONTACT)
        QUALIFICATION FEE NEGATIVE (CONTACT)
        QUALIFICATION FEE ZERO (CONTACT)
        QUALIFICATION FEE HALF FEE (CONTACT)
        QUALIFICATION FEE ABNORMAL (CONTACT)
        QUALIFICATION FEE OVERCHARGE (CONTACT)
        QUALIFICATION FEE SECONDARY QUAL OVERCHARGE (CONTACT)
    STUDENT FEE BURSARY TESTS
        BURSARY TEST BURSARY NOT CANCELLED
ENVIRONMENT RESET
END

COMMENT ENVIRONMENT
COMMENT ****************************************************************************************************

DO _Password
SET SAFETY OFF

COMMENT STUDENT FEE INCOME TESTS - REGISTRATION FEES
COMMENT ****************************************************************************************************

COMMENT REGISTRATION FEE ABNORMAL (CONTACT)

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_fee" TO "STUD_FEE_TEST_REG_FEE_ABNORMAL.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X010ex_Regfee_abnormal ) END_QUERY 
OPEN STUD_FEE_TEST_REG_FEE_ABNORMAL
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "82237@eu"
CLOSE PRIMARY

COMMENT REGISTRATION FEE NEGATIVE (CONTACT)

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_fee" TO "STUD_FEE_TEST_REG_FEE_NEGATIVE.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X010bx_Regfee_negative ) END_QUERY 
OPEN STUD_FEE_TEST_REG_FEE_NEGATIVE
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "82929@eu"
CLOSE PRIMARY

COMMENT REGISTRATION FEE NULL (CONTACT)

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_fee" TO "STUD_FEE_TEST_REG_FEE_NULL.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X010ax_Regfee_null ) END_QUERY 
OPEN STUD_FEE_TEST_REG_FEE_NULL
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "82928@eu"
CLOSE PRIMARY

COMMENT REGISTRATION FEE ZERO (CONTACT)

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_fee" TO "STUD_FEE_TEST_REG_FEE_ZERO.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X010cx_reg_fee_zero ) END_QUERY 
OPEN STUD_FEE_TEST_REG_FEE_ZERO
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "90531@eu"
CLOSE PRIMARY

COMMENT STUDENT FEE INCOME TESTS - QUALIFICATION FEES
COMMENT ****************************************************************************************************

COMMENT QUALIFICATION FEE NULL (CONTACT)

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_fee" TO "STUD_FEE_TEST_QUAL_FEE_NOTRANS.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X021bx_Qual_nofee_transaction ) END_QUERY 
OPEN STUD_FEE_TEST_QUAL_FEE_NOTRANS
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "85515@eu"
CLOSE PRIMARY

COMMENT QUALIFICATION FEE NEGATIVE (CONTACT)

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_fee" TO "STUD_FEE_TEST_QUAL_FEE_NEGATIVETRANS.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X021dx_Qual_negativefee_transaction ) END_QUERY 
OPEN STUD_FEE_TEST_QUAL_FEE_NEGATIVETRANS
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "85682@eu"
CLOSE PRIMARY

COMMENT QUALIFICATION FEE ZERO (CONTACT)

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_fee" TO "STUD_FEE_TEST_QUAL_FEE_ZEROTRANS.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X021cx_Qual_zerofee_transaction ) END_QUERY 
OPEN STUD_FEE_TEST_QUAL_FEE_ZEROTRANS
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "85542@eu"
CLOSE PRIMARY

COMMENT QUALIFICATION FEE HALF FEE (CONTACT)

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_fee" TO "STUD_FEE_TEST_QUAL_FEE_HALFTRANS.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X021ex_Qual_halffee_transaction ) END_QUERY 
OPEN STUD_FEE_TEST_QUAL_FEE_HALFTRANS
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "85740@eu"
CLOSE PRIMARY

COMMENT QUALIFICATION FEE ABNORMAL (CONTACT)

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_fee" TO "STUD_FEE_TEST_QUAL_FEE_ABNORMAL.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X021fx_Qual_abnormalfee_transaction ) END_QUERY 
OPEN STUD_FEE_TEST_QUAL_FEE_ABNORMAL
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "90394@eu"
CLOSE PRIMARY

COMMENT QUALIFICATION FEE OVERCHARGE (CONTACT)

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_fee" TO "STUD_FEE_TEST_QUAL_FEE_OVERCHARGE.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X021gx_qual_fee_overcharge ) END_QUERY 
OPEN STUD_FEE_TEST_QUAL_FEE_OVERCHARGE
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "90401@eu"
CLOSE PRIMARY

COMMENT QUALIFICATION FEE SECONDARY QUAL OVERCHARGE (CONTACT)

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_fee" TO "STUD_FEE_TEST_SECQUAL_FEE_OVERCHARGE.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X021hx_secqual_fee_overcharge ) END_QUERY 
OPEN STUD_FEE_TEST_SECQUAL_FEE_OVERCHARGE
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "90612@eu"
CLOSE PRIMARY

COMMENT STUDENT FEE BURSARY TESTS
COMMENT ****************************************************************************************************

COMMENT BURSARY TEST BURSARY NOT CANCELLED

ACCESSDATA64 ODBC "Dsn" NAME "Sqlite_vss_test_bursary" TO "BURS_FEE_TEST_NOT_CANCELLED.FIL" CHARMAX 100 MEMOMAX 100 
 SOURCE( )
 SQL_QUERY( SELECT * FROM X005ax_bursary_not_cancelled ) END_QUERY 
OPEN BURS_FEE_TEST_NOT_CANCELLED
COUNT
IF COUNT1 > 0 EXPORT FIELDS ALL ACLGRC PASSWORD 1 TO "195734@eu"
CLOSE PRIMARY

COMMENT ENVIRONMENT RESET
COMMENT ****************************************************************************************************

CLOSE PRIMARY
SET SAFETY ON
